# Houston
Avionics Ground station

## Install Instructions
1. You need Python3, so install that if you dont have it,<br> verify you have it by typing ```python3 --version``` in the terminal
2. Run: ```pip install PyQt6``` to install the gui library we will be using
